# TODO

- [x] Update `src/Home.tsx` to use React Router navigation (`useNavigate`) instead of `window.location.href`.
- [x] Wire inactive Home page buttons/CTAs/links to working handlers without changing CSS classes or visual design.
- [x] Keep SignIn/SignUp style untouched; only adjust logic if required after validation.
- [ ] Run build to validate TypeScript and routing integrity.
- [ ] Mark completed tasks.
